{
  "name": "@discord.ts-monorepo/database",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "db:generate": "prisma generate",
    "db:push": "prisma db push --skip-generate"
  },
  "dependencies": {
    "@prisma/client": "^4.10.0",
    "tslib": "^2.5.0"
  },
  "devDependencies": {
    "prisma": "^4.10.0"
  }
}
